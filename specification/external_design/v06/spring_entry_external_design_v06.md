# V6 外部設計

受付要求は受付番号、商品コード、被保険者年齢、保険金額を持つ。COBOL向け固定長は80文字で、1-10受付番号、11-12商品、13-15年齢、16-25金額、26-80予約領域とする。

応答は `accepted`、`validationMessages`、`batchLane` を返す。`batchLane` は金額20,000,000以上で `HIGH`、それ以外で `NORMAL` とする。
