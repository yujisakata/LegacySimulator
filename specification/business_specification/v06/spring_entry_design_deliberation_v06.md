# V6 設計判断記録

| 案 | 内容 | 判断 |
|---|---|---|
| A | 査定バッチも同時にJava化 | 移行範囲と検証量が期限に合わない |
| B（採用） | Springで受付し夜間ファイルでCOBOLへ渡す | 利用者価値を先行し、既存査定を維持できる |
| C | 画面だけWeb化し点検は夜間 | 即時点検の事業要求を満たさない |

性能対策は全ジョブ拡張ではなく、高額案件を後続ジョブへ分離する案を採用した。通常案件の翌朝処理を優先できるためである。
