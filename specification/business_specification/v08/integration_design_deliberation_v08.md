# V8 設計判断記録

| 案 | 内容 | 判断 |
|---|---|---|
| A | 全接続先に単一通信方式を要求 | リリース日と技術制約が異なり採用不可 |
| B（採用） | 接続先別アダプタ＋共通中間形式＋再送キュー | 中核形式を一つに保ち接続先差分を局所化できる |
| C | 接続先形式をCOBOLへ直接追加 | 中核への変更と調整範囲が拡大する |

自動再送は運用承認を伴うため、最大回数と順序を設定として明示し、重複排除後にだけ結果を反映する。
